# This workflow will build a .NET project
# For more information see: https://docs.codeberg.org/ci/actions/

name: .NET

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: docker

    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore dependencies
        run: dotnet restore
      - name: Build
        run: dotnet build --no-restore

  test:
    runs-on: docker
    services:
      postgres:
        image: timescale/timescaledb:latest-pg17
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ttx_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:latest
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore dependencies
        run: dotnet restore
      - name: Test
        env:
          TTX__Infrastructure__ConnectionStrings__Postgres: "Server=postgres;Port=5432;Database=ttx_test;Username=postgres;Password=postgres;"
          TTX__Infrastructure__ConnectionStrings__Redis: "redis:6379"
          TTX__Infrastructure__Events: "Redis"
          TTX__Infrastructure__Data: "Postgres"
        run: dotnet test --verbosity normal
