# Build artifacts (never copy host target dirs into the build).
target
**/target

# VCS + the C# reference project (not needed for the Rust build).
.git
ref

# The Deno web app builds from its own context (src/web/Dockerfile).
src/web/node_modules
src/web/_fresh
src/web/vendor

# Local env files — configuration is provided via real environment variables.
**/.env
