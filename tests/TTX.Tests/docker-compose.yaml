services:
  postgres:
    image: timescale/timescaledb:latest-pg17
    volumes:
      - pg-data:/var/lib/postgresql/data
    env_file:
      - .env
    ports:
      - "5432:5432"

volumes:
  pg-data: {}
